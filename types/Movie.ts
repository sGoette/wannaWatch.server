export type Movie = {
    id: number
    title: string
    file_location: string
    length_in_seconds: number
    width: number
    height: number
    codec: string
    format: string
    poster_file_name: string
    library_id: number
}